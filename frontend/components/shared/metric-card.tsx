/**
 * Shared MetricCard — re-exports the platform metric card so dashboards and
 * modules can import a single shared primitive.
 */
export { MetricCard, type Metric } from "@/components/platform/metric-card";
